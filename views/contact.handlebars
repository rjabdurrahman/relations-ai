<style>
    .contact-form {
        display: flex;
        justify-content: center;
    }
</style>
<script id="00001955-5wxaf3vgoj">
    (function (au, to, mi, zy) {
        var xhr = new XMLHttpRequest();
        xhr.onreadystatechange = function () {
            if (this.readyState == 4 && this.status == 200) {
                mi = to.createElement("div");
                zy = to.getElementById("00001955-5wxaf3vgoj");
                zy.parentNode.insertBefore(mi, zy.nextSibling);
                mi.innerHTML = this.responseText;
                $('form#automizy-form-vLXKYftjGsOMmaRYrhH62QVsKEI').parent().addClass('contact-form first-section');
                var scripts = Array.prototype.slice.call(mi.getElementsByTagName("script"));
                for (var i = 0; i < scripts.length; i++) {
                    if (scripts[i].src != "") {
                        var tag = document.createElement("script");
                        tag.src = scripts[i].src;
                        tag.async = true;
                        document.getElementsByTagName("head")[0].appendChild(tag);
                    } else {
                        eval(scripts[i].innerHTML);
                    }
                }
            }
        };
        xhr.open("GET", "https://gateway.automizy.com/v2/forms/embed/8c958bbbAb9b1A4f7fA8df0Acf08dadce031/vLXKYftjGsOMmaRYrhH62QVsKEI?frameless=1");
        xhr.responseType = "text";
        xhr.send();
    })(window, document)
</script>